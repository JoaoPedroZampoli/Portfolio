import { subtitle, title } from "@/components/primitives";

export default function BlogPage() {
  return (
    <div>
      <h1 className={title()}>Blog</h1>
      <h2 className={subtitle()}>Ainda em construção e concepção...</h2>
    </div>
  );
}
